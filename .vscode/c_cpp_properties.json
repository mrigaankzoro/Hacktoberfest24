<script>
  const configurations = [
    {
      name: "windows-clang-x64",
      includePath: [
        "${workspaceFolder}/**"
      ],
      compilerPath: "clang",
      cStandard: "c11", // Specify a default C standard (change if needed)
      cppStandard: "c++17", // Specify a default C++ standard (change if needed)
      intelliSenseMode: "windows-clang-x64",
      compilerArgs: [
        // Add any additional compiler arguments here
      ]
    }
  ];
  const version = 4; // Version of the configuration format
</script>
